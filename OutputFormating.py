# Some time we want our o/p to be printed as we want so for this we use 
# string.format() just like java

x = 5;
y = 10;
print('The 2 nums are x is {} and y is {}'.format(x, y));
# Just like java script;